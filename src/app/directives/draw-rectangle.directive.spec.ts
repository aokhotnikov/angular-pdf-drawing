import { DrawRectangleDirective } from './draw-rectangle.directive';

describe('DrawRectangleDirective', () => {
  it('should create an instance', () => {
    // const directive = new DrawRectangleDirective();
    // expect(directive).toBeTruthy();
  });
});
